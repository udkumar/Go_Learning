package main // go program consist of packages, and start with main

//import a library/package, here we import fmt, which bring standard formatted I/O  string from C
import (
	"fmt"
)

// Syntax for a function in Go, 
func main(){
	// Println is a function from the fmt library
	fmt.Println("Welcome to GoLang!")
}
